# QA-C10-03.1 — Semántica y trazabilidad de intención

Fecha: 2026-08-10  
Rama: `agent/c10-03-1-intent-semantics`  
Estado: IMPLEMENTADO — pendiente de CI y revisión

## Cambios verificados

- `OrderSource` incorpora `PUBLIC_MENU` mediante migración compatible.
- `createPublicOrderIntent` registra `PUBLIC_MENU`.
- El alta manual conserva `WHATSAPP` por defecto.
- La bandeja administrativa muestra `Menú web · WhatsApp pendiente` para intenciones públicas.
- Se mantiene `RECEIVED`; no se agregó expiración, cron ni estado `ABANDONED`.
- La idempotencia por `clientReference` permanece sin cambios.

## Evidencia local

- Migración Prisma aplicada: `pnpm prisma migrate deploy` — correcta.
- Prisma Client regenerado después de la migración: `pnpm prisma generate` — correcto.
- POST real por LAN a `/api/orders/intents`: `201 Created`; pedido `TL-0010` persistido como `PUBLIC_MENU / RECEIVED`.
- Prueba browser por LAN: abre `api.whatsapp.com` con el destino `5492615956912` y el pedido prellenado.
- Tests: 10 archivos, 24 tests — correctos.
- Typecheck: ejecución local excedió el timeout del entorno sin emitir errores.
- Lint: ejecución local excedió el timeout del entorno sin emitir errores.

## Gate

La aprobación queda pendiente de CI, que debe validar typecheck, lint, tests, audit y build. Si CI es verde, C10-03.1 queda listo para revisión visual de la bandeja y posterior C10-04.
