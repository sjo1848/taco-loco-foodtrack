# QA C8-04 — Bandeja, detalle y acciones operativas

Estado: completado localmente — 2026-08-09.

## Implementado

- Bandeja administrativa en `/admin/orders` con búsqueda por identificador, cliente o teléfono y filtro por estado.
- Alta visual en `/admin/orders/new`, reutilizando productos y modificadores del catálogo.
- Detalle protegido en `/admin/orders/[id]` con contacto, modalidad, mesa, líneas, snapshots, total y observaciones.
- Timeline de eventos con actor, fecha, transición y motivo.
- Acciones de estado en pantalla, con motivo obligatorio para cancelación.
- Endpoints protegidos: listado/alta en `/api/admin/orders` y detalle/transición en `/api/admin/orders/[id]`.
- Estado visible mediante texto y color; layout responsive para panel operativo.

## Evidencia local

- Login administrativo y navegación a la bandeja verificados en navegador real.
- La bandeja mostró el pedido demo `TL-0001`, filtros y búsqueda disponibles.
- Alta visual verificada con `Taco x2 común` y salsa Cheddar obligatoria.
- El detalle mostró snapshot de producto/modificador y evento inicial.
- Transición visual `RECIBIDO → CONFIRMADO` verificada junto con el nuevo evento de historial.
- Pedido temporal `QA C8-04` eliminado después de la prueba; el pedido demo del seed se preservó.
- `pnpm typecheck`, `pnpm test` (8 suites, 17 tests) y `pnpm lint`: aprobados.

## Pendientes para C8-05

- Estados de carga y error más exhaustivos.
- Protección explícita contra doble envío visual.
- Pruebas de sesión expirada y errores de red.
- Evidencia responsive capturada y regresión integral C7/C8.
