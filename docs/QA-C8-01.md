# QA C8-01 — Contrato y esquema de pedidos

Fecha: 2026-08-09  
Estado: completado localmente.

## Implementado

- `Order` con identificador legible incremental, estado, modalidad, contacto, totales y timestamps.
- `OrderLine` con snapshot de producto, precio, cantidad y modificadores.
- `OrderEvent` con estado anterior/nuevo, actor, fecha y motivo.
- Enums de estado, modalidad y origen WhatsApp.
- Migración Prisma `20260809191538_c8_orders`.
- Validación Zod de alta manual.
- Cálculo de totales.
- Máquina de transiciones válidas.
- Seed idempotente con un pedido `TL-0001` de demostración.

## Evidencia

- `pnpm prisma migrate status`: base local sincronizada; 2 migraciones aplicadas.
- `pnpm db:seed`: completado correctamente y reutilizable sin duplicar el pedido demo.
- Consulta PostgreSQL local: 1 pedido `RECEIVED`, 1 línea y 1 evento inicial.
- `pnpm typecheck`: aprobado.
- `pnpm test`: 7 suites, 14 tests aprobados.
- `pnpm lint`: aprobado.

## Criterio de salida

C8-01 queda cerrado. El siguiente bloque es C8-02: implementar el servicio transaccional de cambio de estado y auditoría sobre este modelo.
