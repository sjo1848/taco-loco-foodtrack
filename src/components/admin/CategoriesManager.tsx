"use client";
import { useState } from "react";
type Category = { id: string; name: string; slug: string; active: boolean; sortOrder: number };
export function CategoriesManager({ initial }: { initial: Category[] }) { const [items, setItems] = useState(initial); const [name, setName] = useState(""); const [error, setError] = useState(""); async function add() { setError(""); const slug = name.trim().toLowerCase().normalize("NFD").replace(/[\u0300-\u036f]/g, "").replaceAll(" ", "-"); const response = await fetch("/api/admin/categories", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ name, slug, sortOrder: items.length }) }); if (!response.ok) { setError("No se pudo crear la categoría."); return; } const item = await response.json(); setItems([...items, item]); setName(""); } async function toggle(item: Category) { const response = await fetch(`/api/admin/categories/${item.id}`, { method: "PATCH", headers: { "content-type": "application/json" }, body: JSON.stringify({ active: !item.active }) }); if (response.ok) setItems(items.map((current) => current.id === item.id ? { ...current, active: !item.active } : current)); } return <section className="admin-panel"><div className="admin-inline-form"><input aria-label="Nueva categoría" placeholder="Nueva categoría" value={name} onChange={(event) => setName(event.target.value)} /><button className="admin-button admin-button--primary" onClick={add} disabled={!name.trim()}>Agregar</button></div>{error && <p className="form-error">{error}</p>}<div className="admin-product-list">{items.map((item) => <div className="admin-product-row" key={item.id}><strong>{item.name}</strong><button className="admin-toggle" onClick={() => toggle(item)}>{item.active ? "Activa" : "Inactiva"}</button></div>)}</div></section>; }
